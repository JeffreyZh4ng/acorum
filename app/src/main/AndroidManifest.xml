<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.illinois.finalproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.CreateAccountActivity" />
        <activity android:name=".activities.ForgotPasswordActivity" />
        <activity android:name=".activities.UserDashboardActivity" />
        <activity android:name=".activities.ProfileSettingsActivity" />
        <activity android:name=".activities.ProfileActivity" />
        <activity android:name=".activities.EnrollActivity" />
        <activity android:name=".activities.RegisterCourseActivity" />
        <activity android:name=".listelements.CourseListElement" />
        <activity
            android:name=".activities.CourseDashboardActivity"
            android:label="@string/title_activity_course_dashboard"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".listelements.AnnouncementListElement"></activity>
    </application>

</manifest>