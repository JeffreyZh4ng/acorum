<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.illinois.finalproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateAccountActivity" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".UserDashboardActivity" />
        <activity android:name=".ProfileSettingsActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".EnrollActivity" />
        <activity android:name=".RegisterCourseActivity" />
        <activity android:name=".listelements.CourseListElement" />
        <activity
            android:name=".CourseDashboardActivity"
            android:label="@string/title_activity_course_dashboard"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".tabactivites.AnnouncementActivity"></activity>
    </application>

</manifest>